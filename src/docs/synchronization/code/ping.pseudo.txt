<b>if</b> <span class="hljs-built_in">Base64</span>(<span class="hljs-built_in">HMAC-SHA2-256</span>(body, apikey)) <span class="fsize15">≠</span> headers[<span class="hljs-string">'X-Signature'</span>] <b>then</b>
    log <span class="hljs-string">'Warning: Invalid ping signature!'</span>
    <b>exit</b>
<b>end</b>
ping := <span class="hljs-built_in">json_decode</span>(body)
<b>if</b> ping.seq > localseq <b>then</b> <span class="hljs-comment">// Pull changes with a sequence request</span>
